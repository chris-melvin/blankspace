export type TokenMap = {
  background: string;
  surface: string;
  surfaceSubtle: string;
  surfaceStrong: string;
  textPrimary: string;
  textMuted: string;
  primary: string;
  secondary: string;
  accent: string;
  accentHover: string;
  border: string;
  chart: string[];
  multi?: boolean;
};
